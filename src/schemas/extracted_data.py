from pydantic import BaseModel


class ExtractedData(BaseModel):
    """
    Schema for the data extracted by the ETL process.
    Define the fields according to the structure of the extracted data.
    """

    # Example field. Replace it with actual fields.
